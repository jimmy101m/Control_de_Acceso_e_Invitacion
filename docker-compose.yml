services:
  db:
    image: postgres:latest
    container_name: fraccionamiento_db
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: fraccionamiento_db
    ports:
      - "5432:5432"
